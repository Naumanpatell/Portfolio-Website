services:
  - type: web
    name: NaumanPatel-portfolio
    env: python
    plan: free
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: EMAIL_ADDRESS
        value: nauman0504@gmail.com
      - key: EMAIL_PASSWORD
        sync: false
    healthCheckPath: /api/health
